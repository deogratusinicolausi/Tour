name: turiva_functions
description: TURIVA Cloud Functions
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  firebase_functions: ^0.6.0
  google_cloud_firestore: ^0.5.4
  http: ^1.6.0

dev_dependencies:
  build_runner: ^2.15.1
  lints: ^6.1.0
